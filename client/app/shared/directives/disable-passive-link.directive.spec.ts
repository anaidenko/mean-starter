import { DisablePassiveLinkDirective } from './disable-passive-link.directive';

describe('DisablePassiveLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new DisablePassiveLinkDirective();
    expect(directive).toBeTruthy();
  });
});
